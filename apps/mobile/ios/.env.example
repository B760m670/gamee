# No backend environment variables.
#
# This app no longer talks to any server (Supabase / Cloudflare Worker were
# removed). It is moving to a pure peer-to-peer, end-to-end-encrypted core,
# so there are no cloud URLs or keys to configure here. Any P2P bootstrap
# parameters will be documented when the native core is added.
