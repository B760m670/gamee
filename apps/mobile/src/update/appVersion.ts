// The version this build identifies as. Must match the android versionName in
// android/app/build.gradle and the release tag (without the leading "v").
// Bump this together with versionCode for every release.
export const APP_VERSION = '0.2.0';

// Where releases are published (used to look up the latest APK).
export const REPO = 'B760m670/gamee';
